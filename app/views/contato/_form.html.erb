<%= form_for(@contact, multipart: true) do |f| %>
       <% if @contact.errors.any? %>
         <div id="error_explanation">
         <h2><%= pluralize(@contact.errors.count, "error") %> prohibited this friend from being saved:</h2> 
           <ul>
           <% @contact.errors.full_messages.each do |msg| %>
             <li><%= msg %></li>
           <% end %>
          </ul>
        </div>
      <% end %>
    
      <div class="field">
        <%= f.label :name %>
        <%= f.text_field :name %>
      </div>
      <div class="field">
        <%= f.label :endereco %>
        <%= f.file_field :endereco %>
      </div>
      <div class="field">
        <%= f.label :email %>
        <%= f.file_field :email %>
      </div>
      <div class="field">
        <%= f.label :telefone %>
        <%= f.file_field :telefone %>
      </div>
      <div class="actions">
        <%= f.submit 'Adicionar' %>
        <%= link_to 'Cancelar', contacts_path, class: 'button' %>
      </div>
    <% end %>
